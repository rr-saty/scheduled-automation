# Scheduled Automation Logger

This GitHub Actions project automatically runs every 10 minutes.

Each run adds the current UTC date and time to `logs/automation-log.txt`.

You can also run it manually from the Actions tab using the **Run workflow** button.
